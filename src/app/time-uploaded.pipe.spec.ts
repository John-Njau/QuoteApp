import { TimeUploadedPipe } from './time-uploaded.pipe';

describe('TimeUploadedPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeUploadedPipe();
    expect(pipe).toBeTruthy();
  });
});
